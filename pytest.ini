[pytest]
addopts = --cov=app --cov=db_query --cov-report=term-missing --cov-branch -rs
markers =
    smoke: quick smoke tests
    intent: intent parsing tests
    codegen: deterministic code template tests 