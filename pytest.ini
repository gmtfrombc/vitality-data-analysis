[pytest]
addopts = --cov=app --cov=db_query --cov-report=term-missing --cov-branch --cov-fail-under=60 -rs
markers =
    smoke: quick smoke tests
    intent: intent parsing tests
    codegen: deterministic code template tests
    online: live external-service integration tests (opt-in)
    validation: data validation-engine tests 